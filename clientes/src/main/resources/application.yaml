server:
  port: 8080
#  port: 8083

spring:
  application:
    name: microservices_clientes

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: tata
    password: tata123

  datasource:
    url: jdbc:postgresql://postgres:5432/clientes_db
#    url: jdbc:postgresql://localhost:5432/clientes_db
    username: "tata"
    password: "tata123"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true